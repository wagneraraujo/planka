# Application Settings
NODE_ENV=production
BASE_URL=http://localhost:3000
SECRET_KEY=changethisinproduction
TRUST_PROXY=0
TOKEN_EXPIRES_IN=365

# Database Configuration
POSTGRES_PASSWORD=secretpassword
POSTGRES_USER=postgres
POSTGRES_DB=planka

# Admin User Configuration (Optional)
DEFAULT_ADMIN_EMAIL=admin@example.com
DEFAULT_ADMIN_PASSWORD=adminpassword
DEFAULT_ADMIN_NAME=Admin User
DEFAULT_ADMIN_USERNAME=admin

# Security Settings
SHOW_DETAILED_AUTH_ERRORS=false
ALLOW_ALL_TO_CREATE_PROJECTS=false

# S3 Storage Configuration (Optional)
S3_ENDPOINT=
S3_REGION=
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_BUCKET=
S3_FORCE_PATH_STYLE=true

# OIDC Configuration (Optional)
OIDC_ISSUER=
OIDC_CLIENT_ID=
OIDC_CLIENT_SECRET=
OIDC_SCOPES=openid email profile
OIDC_RESPONSE_MODE=fragment
OIDC_USE_DEFAULT_RESPONSE_MODE=true
OIDC_ADMIN_ROLES=admin
OIDC_CLAIMS_SOURCE=userinfo
OIDC_EMAIL_ATTRIBUTE=email
OIDC_NAME_ATTRIBUTE=name
OIDC_USERNAME_ATTRIBUTE=preferred_username
OIDC_ROLES_ATTRIBUTE=groups
OIDC_IGNORE_USERNAME=false
OIDC_IGNORE_ROLES=false
OIDC_ENFORCED=false

# Email Notifications (Optional)
SMTP_HOST=
SMTP_PORT=587
SMTP_NAME=
SMTP_SECURE=true
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM="Planka Admin <noreply@example.com>"
SMTP_TLS_REJECT_UNAUTHORIZED=true

# Integration Webhooks (Optional)
# Format: [{url: string, accessToken?: string, events?: string[], excludedEvents?: string[]}]
WEBHOOKS=

# Chat Integration (Optional)
SLACK_BOT_TOKEN=
SLACK_CHANNEL_ID=
GOOGLE_CHAT_WEBHOOK_URL=
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
TELEGRAM_THREAD_ID=
